## Todo
- Broken ssh setup. 
    - keys aren't automatically added to the agent on boot. 
    - keys don't have passwords. 
    - the ssh agent that is used is in /usr/bin (macOs openssh port) while other ssh commands are run from Homebrew's openssh.

